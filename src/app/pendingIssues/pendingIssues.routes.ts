import { Routes } from '@angular/router';

import { pendingIssuesComponent } from './pendingIssues.component';

export const pendingIssuesRoutes: Routes = [
	{ path: 'pending-issues',  component: pendingIssuesComponent }
];
